[color]
    branch = auto
    diff = auto
    status = auto
[user]
    name = Giuseppe Capizzi
    email = g.capizzi@gmail.com
[core]
    editor = /usr/bin/vim
    excludesfile = ~/.cvsignore
    autocrlf = input
[alias]
    st = status
    ci = commit
    br = branch
    co = checkout
    df = diff
    dc = diff --cached
    lg = "!. ~/.dotfiles/githelpers && pretty_git_log"
    lga = "!git lg --branches --remotes --tags"
    td = "!git lg --since='yesterday'"
    tdf = diff "@{yesterday}"
    who = shortlog -s --
    aa = add -A
[merge]
    renamelimit = 3315
[diff]
    ignoreSubmodules = dirty
[credential]
    helper = osxkeychain
[push]
    default = simple
[pull]
    rebase = true
[status]
    short = 1
    branch = 1
